// import { StyleSheet, Text, View } from 'react-native';
import GlobalContainer from '../components/Globalcontainer';
import GlobalText from '../components/GlobalText';


const GamesScreen = () => {
  return (
    <GlobalContainer>
      <GlobalText>Games</GlobalText>
    </GlobalContainer>
  );
}

export default GamesScreen;
