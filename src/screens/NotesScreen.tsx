import GlobalContainer from '../components/Globalcontainer';
import GlobalText from '../components/GlobalText';


const NotesScreen = () => {
  return (
    <GlobalContainer>
      <GlobalText>Note List</GlobalText>
    </GlobalContainer>
  );
}

export default NotesScreen;
